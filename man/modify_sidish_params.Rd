% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_modify_sidish_params.R
\name{modify_sidish_params}
\alias{modify_sidish_params}
\title{Modify SIDISH parameters with user-provided values}
\usage{
modify_sidish_params(usr_list = list())
}
\arguments{
\item{usr_list}{A named list of parameters to override the defaults. Only parameters that exist in the default list will be modified.}
}
\value{
A list containing the merged parameters with user-provided values overriding the defaults.
}
\description{
This function provides a default set of parameters for the SIDISH method and merges them
with any user-provided parameters. The parameters are organized into several categories:
preprocessing, VAE training, Deep Cox training, training/risk definition, and execution environment.
}
\section{Default Parameters}{

The following parameters can be passed via \code{...}. Defaults are inherited from
the Python SIDISH implementation.

Preprocessing parameters:
\itemize{
\item \code{patient_id}: Column name for patient/sample identifiers in phenotype data
(default: \code{"Sample"})
\item \code{celltype_name}: Column name for cell type annotations in single-cell metadata.
Mapped from R's \code{label_type} parameter (default: \code{"celltype_major"})
\item \code{processed}: Whether input expression matrices are already normalized/log-transformed
(default: \code{TRUE})
\item \code{use_spatial_graph}: Whether to incorporate spatial neighborhood information
(requires spatial coordinates) (default: \code{FALSE})
\item \code{k_neighbors}: Number of neighbors for spatial graph construction.
Ignored when \code{use_spatial_graph = FALSE} (default: \code{NULL})
}

VAE Training (Phase 1) parameters:
\itemize{
\item \code{phase1_epochs}: Total number of epochs for initial VAE training (default: \code{225L})
\item \code{phase1_i_epochs}: Number of epochs for VAE retraining iterations (default: \code{20L})
\item \code{phase1_latent_size}: Dimensionality of the latent space representation (default: \code{32L})
\item \code{phase1_layer_dims}: Hidden layer dimensions for encoder/decoder networks
(default: \code{c(512L, 128L)})
\item \code{phase1_batch_size}: Mini-batch size for VAE training (default: \code{256L})
\item \code{phase1_optimizer}: Optimizer algorithm for VAE training (default: \code{"Adam"})
\item \code{phase1_lr}: Initial learning rate for VAE training (default: \code{1.0e-4})
\item \code{phase1_lr_3}: Learning rate for later VAE training iterations (default: \code{1.0e-4})
\item \code{phase1_dropout}: Dropout rate for VAE layers (default: \code{0L})
\item \code{phase1_type}: VAE architecture type: \code{"Normal"} (standard) or \code{"Dense"}
(dense representation) (default: \code{"Normal"})
}

Deep Cox Training (Phase 2) parameters:
\itemize{
\item \code{phase2_epochs}: Number of training epochs for Deep Cox proportional hazards model
(default: \code{500L})
\item \code{phase2_hidden}: Number of neurons in hidden layer of Deep Cox network
(default: \code{128L})
\item \code{phase2_lr}: Learning rate for Deep Cox model training (default: \code{1.0e-4})
\item \code{phase2_dropout}: Dropout rate for Deep Cox layers (default: \code{0L})
\item \code{phase2_test_size}: Proportion of samples reserved for validation/testing
(default: \code{0.2})
\item \code{phase2_batch_size_bulk}: Mini-batch size for bulk data during Deep Cox training
(default: \code{256L})
}

Training & Risk Definition parameters:
\itemize{
\item \code{train_iterations}: Number of iterative training cycles to refine cell risk scores
(default: \code{5L})
\item \code{train_percentile}: Percentile threshold for defining high-risk cells
(e.g., 0.95 = top 5\\% riskiest cells) (default: \code{0.95})
\item \code{train_steepness}: Scaling factor controlling steepness of risk score updates
during iterations (default: \code{30L})
\item \code{train_path}: Directory path for saving model checkpoints and intermediate results
(default: \code{"./"})
\item \code{train_num_workers}: Number of parallel workers for data loading
(set to 0 for reproducibility) (default: \code{0L})
\item \code{train_distribution_fit}: Method for fitting risk score distribution:
\code{"fitted"} (empirical) or \code{"default"} (theoretical) (default: \code{"fitted"})
}

Execution Environment parameters:
\itemize{
\item \code{device}: Computation device: \code{"cuda"} (GPU) or \code{"cpu"}
(default: \code{"cuda"})
\item \code{seed}: Random seed for reproducibility (synchronized across R/Python/PyTorch)
(default: \code{123L})
\item \code{verbose}: Whether to display training progress and diagnostics (default: \code{TRUE})
}
}

